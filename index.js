/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {MainScreen} from './SplashScreen/screens';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => MainScreen);
